import styled from 'styled-components';

export const Root = styled.button`
  background-color: var(--secendary);
  color: var(--accents-1);
  cursor: pointer;
  display: inline-flex;
  padding: 0 10px;
  border-radius: 5px;
  line-height: 1.5rem;
`;
